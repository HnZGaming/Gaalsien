{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "PowerShell",
        "request": "launch",
        "name": "Start Torch with auto restart",
        "script": "./Torch.Server.exe",
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ]
  }